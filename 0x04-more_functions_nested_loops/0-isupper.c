#include "main.h"
/**
* _upper - function to check for upper case character
*@c: function parameter
* Return: 1 if it's  uppercase
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}

